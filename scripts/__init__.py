"""CLI entry-point scripts for the Insurance AI Knowledge Platform.

Each script is a thin wrapper that wires up a
:class:`~insurance_kb.container.di_container.DIContainer` and invokes the
corresponding use case. In Phase 1, only ``run_pipeline.py`` is functional
(running the Hello Pipeline); the others are placeholders reserved for
later phases.
"""
